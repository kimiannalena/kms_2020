language: node_js
node_js:
  - "stable"

branches:
  only:
    - main

before_install:
  - npm i -g npm@latest
  - npm i

before_script:
  - npm install -g @angular/cli

script:
  - ng build
  - ng lint
